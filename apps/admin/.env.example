# Public browser-safe values
NEXT_PUBLIC_APP_ENV=development
NEXT_PUBLIC_API_BASE_URL=http://localhost:4000

# Server-only placeholders (do not expose in NEXT_PUBLIC_*)
ADMIN_INTERNAL_API_TOKEN=replace-me
